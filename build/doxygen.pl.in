# for vxl files run the vxl_doxy.pl script, and use xipgroup.pl for all other files
if ( $ARGV[0] =~ /(vxl|vcl|vnl)/) 
{
    system ("perl @Xip_SOURCE_DIR@/build/vxl_doxy.pl $ARGV[0]");
}
else
{
    system ("perl @Xip_SOURCE_DIR@/build/xipgroup.pl $ARGV[0]");
}
